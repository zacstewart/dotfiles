if [ -f ~/.bashrc ]; then
   source ~/.bashrc
fi

source ~/.profile

chruby='/usr/local/opt/chruby/share/chruby/auto.sh'
if [ -a "$chruby" ]; then
  source $chruby
fi

gitshot () {
  imagesnap -q -w 3 "$HOME/.gitshots/$(date -j -f "%a %b %d %T %Z %Y" "`date`" "+%s").jpg"
}
